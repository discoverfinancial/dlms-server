{
  "name": "dlms-base",
  "version": "2.0.2",
  "description": "Document Lifecycle Management Service Base Package",
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "scripts": {
    "clean": "rm -rf ./build && rm -rf ./node_modules",
    "prepublish1": "npm run build",
    "build": "npm install && tsc && npm pack .",
    "test": "echo \"Error: no test specified\" && exit 1",
    "package": "npm pack .",
    "check": "NODE_TLS_REJECT_UNAUTHORIZED=0 npm-check -u"
  },
  "contributors": [
    "Keith Smith <KeithSmith@discover.com>",
    "Bryce Curtis <BryceCurtis@discover.com>"
  ],
  "license": "MIT",
  "dependencies": {
    "@types/node": "^24.10.1",
    "typescript": "^5.9.3"
  },
  "devDependencies": {
    "npm-check": "^6.0.1"
  },
  "files": [
    "build"
  ]
}
